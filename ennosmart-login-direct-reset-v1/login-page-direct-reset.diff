--- login-page.tsx
+++ login-page.tsx

+ beginDirectPasswordReset()
+ appelle forgotPassword(email) en arrière-plan
+ si preview_token existe : setMode("reset")

- onClick={() => switchMode("forgot")}
+ onClick={() => void beginDirectPasswordReset()}

- {(mode === "forgot" || mode === "reset") && (
+ {mode === "reset" && (
